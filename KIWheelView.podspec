Pod::Spec.new do |s|
  s.name         = "KIWheelView"
  s.version      = "0.0.1"
  s.summary      = "KIWheelView"
  s.description  = <<-DESC
  					KIWheelView
                   DESC
  s.homepage     = "https://github.com/smartwalle/KIWheelView"
  s.license      = "MIT"
  s.author       = { "SmartWalle" => "smartwalle@gmail.com" }
  s.platform     = :ios, "7.0"
  s.source       = { :git => "https://github.com/smartwalle/KIWheelView.git", :barnch => "master" }
  s.requires_arc = true
end
